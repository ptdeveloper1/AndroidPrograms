<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="projecthomepage.example.com.projecthomepage">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivityHomePage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".SecondPage">
        </activity>
        <activity android:name=".GridWithDates"></activity>
        <activity android:name=".TitlePage"></activity>
        <activity android:name=".AlarmFirstClass"></activity>
        <activity android:name=".RepeatingActivity"></activity>
        <activity android:name=".AlarmGridDates"></activity>
        <receiver android:name=".AlarmSecondClass"></receiver>
        <receiver android:name=".ReminderClass"></receiver>
        <activity android:name=".HabitListActivity"></activity>
    </application>


</manifest>